# TODO - Portfolio Styling Improvements

- [x] Update `src/App.css` to use the design tokens from `src/index.css` (remove clashing base styles).

- [ ] Restyle Projects section to remove bordered/pill “card” look:
  - replace `.project-title` styling
  - make `.project` entries use subtle separators and clean typography
- [ ] Improve navbar/hero/section spacing to look more modern and consistent.
- [ ] (If needed) Update minor markup/classes in `src/sections/Projects.jsx` to support improved styling.
- [ ] Run `npm run dev` (or build) to verify everything renders.

